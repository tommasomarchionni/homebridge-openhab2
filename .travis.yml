language: node_js
node_js:
  - "node"
  - "lts/*"
  - "8"
  - "7"
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libnss-mdns libavahi-compat-libdnssd-dev
cache:
  directories:
    - "node_modules"  
before_script:
  - npm install
script:
  - npm test
notifications:
  email: false
